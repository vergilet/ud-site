window.series_<%= @series.id %> = '<%= escape_javascript(render partial: 'series/cover_ajax', locals: { cover: @series_presenter.cover}) %>';
$(".series_<%= @series.id %>")
  .empty()
  .append(window.series_<%= @series.id %>)
  .fadeIn( 1000 );
